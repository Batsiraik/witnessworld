# EAS Build only loads `.easignore` from the **git repository root** (witnessworld/.easignore).
# Rules for uploads live there so the archive does not include vendor/, api/, uploads/, etc.
# https://docs.expo.dev/build-reference/easignore/
